<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitnessReservatie.UI"
        xmlns:Managers="clr-namespace:FitnessReservatieBL.Managers;assembly=FitnessReservatieBL" x:Class="FitnessReservatie.UI.KlantWindow"
        mc:Ignorable="d"
        Title="My Personal Panel" Height="800" Width="1200">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FFE9A500" Offset="0.557"/>
                <GradientStop Color="#FFF3CA00" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.ColumnSpan="7" Grid.Row="0" Name="LabelWelkomKlant" Content="Welcome Back " HorizontalAlignment="Left" Height="40" Margin="15" VerticalAlignment="Top" Foreground="White" FontFamily="Unispace" FontSize="24"/>
        <Button Grid.Column="6" Grid.Row="1" Content="Make New Reservation" Width="140" Height="34" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="15" Click="Button_Click">
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="15"/>
                </Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Background" Value="#FFF4F4F4"/>
                    <Setter Property="Foreground" Value="#FF323232"/>
                    <Setter Property="Opacity" Value="0.8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter Property="Background" Value="#FF323232"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Opacity" Value="1"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Resources>
        </Button>
        <TabControl Grid.ColumnSpan="7" Grid.Row="2" Margin="15,0,15,15">
            <TabControl.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="0,5,5,5"/>
                </Style>
            </TabControl.Resources>
            <TabItem Header="My Reservations">
                <TabItem.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="5,5,0,0"/>
                    </Style>
                </TabItem.Resources>
                <Grid Background="#FFE5E5E5">
                    <ListView d:ItemsSource="{d:SampleData ItemCount=5}" Foreground="#FF323232" Background="#FFF4F4F4">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>
        </TabControl>
        <ComboBox Grid.Column="3" Grid.Row="1" Name="ComboBox_ToesteltypeSelector" HorizontalAlignment="Center" VerticalAlignment="Center" Width="120" Margin="15"/>
        <DatePicker Grid.Column="1" Grid.Row="1" HorizontalAlignment="Left" Width="120" Margin="15" VerticalAlignment="Center" DisplayDate="2022-05-09" FirstDayOfWeek="Monday"/>
        <ComboBox Grid.Column="5" Grid.Row="1" Name="ComboBox_TijdslotSelector" HorizontalAlignment="Center" VerticalAlignment="Center" Width="120" Margin="15"/>
    </Grid>
</Window>
